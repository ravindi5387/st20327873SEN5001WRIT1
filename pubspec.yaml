name: university_event_management
description: University Event Management System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  intl: ^0.18.1
  firebase_core: ^3.6.0
  firebase_database: 11.3.10
  firebase_auth: ^5.3.3
  http: ^1.1.0  # ✅ SAFE to add - no conflicts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true